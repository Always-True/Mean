/* register the modules the application depends upon here*/
angular.module('ducs', []);

/* register the application and inject all the necessary dependencies */
var app = angular.module('ducApp', ['ducs']);